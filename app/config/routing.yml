app:
    # loads routes from the PHP annotations of the controllers found in that directory
    resource: '@AppBundle/Controller/'
    type:     annotation
payment_create:
    path:     /payment/create
    defaults: { _controller: AppBundle:Payment:create }